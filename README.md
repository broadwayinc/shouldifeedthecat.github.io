# Should I Feed the Cat

Meet the cat we're feeding. You're welcome to stop by and help keep them fed.

## How to Run

Install dependencies, then start the dev server:

```
npm install
```

```
npm run dev
```

Visit http://localhost:3000 — the app runs on port 3000.